rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Her kullanıcı sadece kendi ev kayıtlarına erişir
    match /users/{userId}/homes/{homeId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
