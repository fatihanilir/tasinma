// Bu dosya daha sonra test eklemek için kullanılacak.
// Firebase mock'ları gerektiğinden şimdilik boş bırakıyoruz.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
